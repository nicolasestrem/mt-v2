---
// Nomination form component for Mobility Trailblazers
import Icons from './Icons.astro';
---

<section class="nomination" id="vorschlagen">
	<!-- Subtle steps divider at top -->
	<div class="section-divider-top">
		<svg viewBox="0 0 1200 80" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
			<path d="M0,40 L300,40 L300,80 L600,80 L600,40 L900,40 L900,80 L1200,80 L1200,0 L0,0 Z" fill="white"></path>
		</svg>
	</div>
	<div class="container">
		<div class="nomination-content">
			<h1 id="nominate-26-in-2026" class="nomination-title" style="color: #004C5F;">Schlagen Sie eine(n) Mobility Trailblazer vor – 26 in 2026</h1>
			
			<div class="nomination-description">
				<p><strong>„Mobiler Wandel braucht Mut. Und mutige Menschen verdienen eine Bühne."</strong></p>
				
				<h2 id="update-25-in-2025">Update: Prozess für 25 in 2025 abgeschlossen</h2>
				<p>Der Prozess für 25 in 2025 ist abgeschlossen. Ab sofort freuen wir uns über Nominierungen für 26 in 2026 – für Menschen, die im DACH-Raum die Mobilitätswende mit Tatkraft voranbringen.</p>
				
				<h2 id="eligibility">Wer kann nominiert werden?</h2>
				<p>Wir suchen Macherinnen und Macher, die</p>
				<ul>
					<li>2026 mit konkreten Projekten oder Initiativen den mobilen Wandel beschleunigen,</li>
					<li>durch Innovationskraft, Umsetzungskompetenz und eine klare Haltung überzeugen,</li>
					<li>die Mobilitätswende aktiv gestalten – branchenunabhängig und hierarchiefrei.</li>
				</ul>
				<p>Im Mittelpunkt steht immer die einzelne Person – nicht das Unternehmen oder die Organisation.</p>
				
				<h2 id="criteria">Unsere Auswahlkriterien</h2>
				<ul>
					<li><strong>Mut & Pioniergeist:</strong> Welche Risiken wurden eingegangen? Welche Konventionen infrage gestellt?</li>
					<li><strong>Innovationsgrad:</strong> Wie neuartig ist der Ansatz im Kontext Mobilität/Verkehrswende?</li>
					<li><strong>Wirkung & Umsetzung:</strong> Welche messbare Veränderung wurde erreicht oder angestoßen?</li>
					<li><strong>Relevanz für die Mobilitätswende:</strong> Trägt der Beitrag strukturell oder gesellschaftlich spürbar bei?</li>
					<li><strong>Vorbildfunktion & Persönlichkeit:</strong> Inspiriert die Person durch Haltung, Sichtbarkeit und Verantwortungsbewusstsein?</li>
				</ul>
				
				<h2 id="how-to-nominate">So funktioniert die Nominierung</h2>
				<p>Füllen Sie das Online-Formular aus und geben Sie uns erste Informationen zur vorgeschlagenen Person.</p>
				<p>Ergänzen Sie gern Links, Medienberichte oder Projektbeschreibungen. Mehrfachnominierungen sind möglich.</p>
				<p><em>Hinweis:</em> Alle Vorschläge werden von unserem Team geprüft. Eine unabhängige Jury wählt 2026 die finalen 26 Mobility Trailblazers. Details zum Zeitplan veröffentlichen wir rechtzeitig.</p>
				
				<h2 id="why-it-matters">Warum Ihre Nominierung wichtig ist</h2>
				<p>Mutige Vorreiterinnen und Vorreiter verdienen Aufmerksamkeit. Mit Ihrer Nominierung helfen Sie, diesen Menschen eine Bühne zu geben – und andere zu ermutigen, eigene Wege in der Mobilitätswende zu gehen.</p>
				
				<h2 id="faq">FAQ zur Nominierung</h2>
				<div class="faq-section">
					<h3 id="faq-who-can-nominate">Wer darf nominieren?</h3>
					<p>Jede Person. Selbstnominierungen sind möglich.</p>
					
					<h3 id="faq-multiple-nominations">Kann ich mehrere Personen nominieren?</h3>
					<p>Ja. Reichen Sie pro Person eine eigene Nominierung ein.</p>
					
					<h3 id="faq-after-submission">Was passiert nach der Einreichung?</h3>
					<p>Wir prüfen alle Vorschläge und informieren die Nominierten bei Rückfragen. Die Jury wählt 2026 die 26 Mobility Trailblazers.</p>
				</div>
				
				<h2 id="cta-nominate-now">Jetzt nominieren</h2>
			</div>
			
			<form class="nomination-form" id="nominationForm">
				<!-- Form status messages -->
				<div class="form-message success-message" id="successMessage" role="alert" aria-live="polite" style="display: none;">
					✓ Vielen Dank für Ihren Vorschlag! Wir werden ihn sorgfältig prüfen.
				</div>
				<div class="form-message error-message" id="errorMessage" role="alert" aria-live="assertive" style="display: none;">
					⚠ <span id="errorText">Es gab einen Fehler beim Senden. Bitte versuchen Sie es erneut.</span>
				</div>
				
				<!-- Web3Forms Configuration -->
				<input type="hidden" name="access_key" value="3bae7bb8-eb67-4656-8ba3-900a0d95306a">
				<input type="hidden" name="subject" value="Neue Mobility Trailblazer Nominierung 2026">
				<input type="hidden" name="from_name" value="Mobility Trailblazers Website">
				
				<!-- Additional Web3Forms fields -->
				<!-- Redirect is commented out to use AJAX submission instead -->
				<!-- <input type="hidden" name="redirect" value="https://mobilitytrailblazers.de/#vorschlagen?success=true"> -->
				
				<!-- Email configuration for better deliverability -->
				<input type="hidden" name="replyto" value="nominatorEmail">
				
				<!-- Specify the email address where submissions should be sent -->
				<input type="hidden" name="email" value="kontakt@mobilitytrailblazers.de">
				
				<!-- Metadata for tracking -->
				<input type="hidden" name="_template" value="table">
				<input type="hidden" name="_captcha" value="false">
				
				<div class="form-row">
					<div class="form-group">
						<label for="nominatorFirstName">Ihr Vorname *</label>
						<input type="text" id="nominatorFirstName" name="nominatorFirstName" required aria-required="true">
					</div>
					
					<div class="form-group">
						<label for="nominatorLastName">Ihr Nachname *</label>
						<input type="text" id="nominatorLastName" name="nominatorLastName" required aria-required="true">
					</div>
				</div>
				
				<div class="form-group">
					<label for="nominatorEmail">Ihre E-Mail *</label>
					<input type="email" id="nominatorEmail" name="nominatorEmail" required aria-required="true" aria-describedby="emailHelp">
					<small id="emailHelp" class="form-help">Wir verwenden Ihre E-Mail nur für Rückfragen zu Ihrer Nominierung.</small>
				</div>
				
				<!-- Honeypot Spam Protection -->
				<input type="checkbox" name="botcheck" class="hidden" style="display: none;">
				
				<div class="form-group">
					<label for="nominatorSalutation">Anrede *</label>
					<select id="nominatorSalutation" name="nominatorSalutation" required aria-required="true">
						<option value="">Bitte wählen</option>
						<option value="Frau">Frau</option>
						<option value="Herr">Herr</option>
						<option value="Keine Angabe">Keine Angabe</option>
					</select>
				</div>
				
				<div class="form-group">
					<label for="nominationMessage">Optionale Nachricht</label>
					<textarea id="nominationMessage" name="nominationMessage" rows="6" placeholder="Wen schlagen Sie für 2026 vor? Gerne mit einer kurzen oder auch ausführlichen Begründung."></textarea>
				</div>
				
				<div class="form-group checkbox-group">
					<label class="checkbox-label">
						<input type="checkbox" name="newsletterSignup" value="true">
						<span class="checkmark"></span>
						Gerne melde ich mich auch zum Newsletter an.
					</label>
				</div>
				
				<button type="submit" class="nomination-btn">Jetzt für 26 in 2026 nominieren</button>
			</form>
		</div>
	</div>
</section>

<style>
	.nomination {
		background: var(--color-background);
		position: relative;
		padding: 3rem 0;
	}

	.section-divider-top {
		position: absolute;
		top: -1px;
		left: 50%;
		transform: translateX(-50%);
		width: 100%;
		max-width: none;
		overflow: hidden;
		line-height: 0;
		z-index: 2;
	}

	.section-divider-top svg {
		position: relative;
		display: block;
		width: 100%;
		height: 40px;
	}

	@media (min-width: 768px) {
		.section-divider-top svg {
			height: 60px;
		}
	}

	.nomination::before {
		content: '';
		position: absolute;
		inset: 0;
		background: url('/images/background.webp') center center/cover;
		opacity: 0.05;
		z-index: 1;
	}

	.nomination-content {
		max-width: 900px;
		margin: 0 auto;
		position: relative;
		z-index: 2;
	}

	/* Update h1 styles for SEO */
	h1.nomination-title {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 2rem;
		text-align: center;
		color: var(--color-primary);
		background: linear-gradient(45deg, var(--color-primary), var(--color-accent));
		background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.nomination-description {
		margin-bottom: 3rem;
		color: var(--color-text);
	}

	.nomination-description p {
		font-size: 1.1rem;
		line-height: 1.6;
		margin-bottom: 1rem;
	}

	/* Ensure proper heading hierarchy */
	.nomination-description h2 {
		color: var(--color-primary);
		font-size: 1.8rem;
		font-weight: 600;
		margin: 2rem 0 1rem;
	}

	.nomination-description h3 {
		color: var(--color-primary);
		font-size: 1.3rem;
		font-weight: 600;
		margin: 1.5rem 0 0.8rem;
	}

	/* FAQ Section Styles */
	.faq-section {
		margin-top: 1.5rem;
		padding: 1.5rem;
		background: rgb(255, 255, 255, 0.5);
		border-radius: 12px;
		border-left: 4px solid var(--color-accent);
	}

	.faq-section h3 {
		font-size: 1.2rem;
		margin-bottom: 0.5rem;
		color: var(--color-primary);
	}

	.faq-section p {
		margin-bottom: 1rem;
		padding-left: 1rem;
	}

	.nomination-description ul {
		list-style: none;
		padding: 0;
		margin: 1rem 0;
	}

	.nomination-description li {
		padding: 0.5rem 0;
		font-size: 1rem;
		position: relative;
		padding-left: 1.5rem;
	}

	.nomination-description li::before {
		content: '•';
		position: absolute;
		left: 0;
		color: var(--color-accent);
		font-weight: bold;
		font-size: 1.2rem;
	}

	.nomination-description strong {
		color: var(--color-primary);
		font-weight: 600;
	}

	.nomination-description em {
		color: var(--color-accent);
		font-style: italic;
	}

	.nomination-form {
		background: #fff;
		border-radius: 20px;
		padding: 2.5rem;
		box-shadow: 
			0 25px 60px rgb(0, 0, 0, 0.15),
			0 0 100px rgb(246, 173, 85, 0.25),
			0 0 150px rgb(26, 54, 93, 0.15),
			inset 0 0 50px rgb(246, 173, 85, 0.03);
		border: 2px solid rgb(246, 173, 85, 0.2);
		animation: subtle-glow 3s ease-in-out infinite alternate;
	}

	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.5rem;
	}

	.form-group {
		display: flex;
		flex-direction: column;
		margin-bottom: 1.5rem;
	}

	.form-group label {
		font-size: 0.9rem;
		font-weight: 600;
		margin-bottom: 0.5rem;
		color: var(--color-primary);
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		padding: 0.8rem 1rem;
		border: 2px solid #e5e7eb;
		border-radius: 8px;
		background: #fff;
		color: var(--color-text);
		font-size: 1rem;
		transition: all 0.3s ease;
		font-family: inherit;
		box-shadow: 
			0 0 8px rgb(26, 54, 93, 0.2),
			0 0 16px rgb(246, 173, 85, 0.15),
			0 0 24px rgb(26, 54, 93, 0.1);
	}

	.form-group textarea {
		resize: vertical;
		min-height: 120px;
	}

	.form-group input:hover,
	.form-group select:hover,
	.form-group textarea:hover {
		border-color: rgb(246, 173, 85, 0.8);
		box-shadow: 
			0 0 12px rgb(26, 54, 93, 0.4),
			0 0 25px rgb(246, 173, 85, 0.35),
			0 0 40px rgb(26, 54, 93, 0.25),
			0 0 55px rgb(246, 173, 85, 0.15);
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: var(--color-accent);
		box-shadow: 
			0 0 0 3px rgb(246, 173, 85, 0.5),
			0 0 18px rgb(26, 54, 93, 0.5),
			0 0 35px rgb(246, 173, 85, 0.4),
			0 0 55px rgb(26, 54, 93, 0.35),
			0 0 75px rgb(246, 173, 85, 0.25),
			inset 0 0 15px rgb(246, 173, 85, 0.08);
	}

	.checkbox-group {
		flex-direction: row;
		align-items: center;
		gap: 0.5rem;
	}

	.form-message {
		padding: 1rem;
		border-radius: 8px;
		margin-bottom: 1.5rem;
		font-size: 1rem;
		text-align: center;
		animation: slide-down 0.3s ease-out;
	}

	.success-message {
		background: #d4edda;
		border: 1px solid #c3e6cb;
		color: #155724;
	}

	.error-message {
		background: #f8d7da;
		border: 1px solid #f5c6cb;
		color: #721c24;
	}

	.form-help {
		font-size: 0.85rem;
		color: #4a5568;
		margin-top: 0.25rem;
		display: block;
	}

	@keyframes slide-down {
		from {
			opacity: 0;
			transform: translateY(-10px);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.checkbox-label {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		cursor: pointer;
		font-size: 1rem;
		margin: 0;
	}

	.checkbox-label input[type="checkbox"] {
		width: 18px;
		height: 18px;
		margin: 0;
	}

	.nomination-btn {
		background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
		color: #fff;
		border: none;
		padding: 1rem 2rem;
		border-radius: 50px;
		font-size: 1rem;
		font-weight: 600;
		cursor: pointer;
		transition: all 0.3s ease;
		width: 100%;
		margin-top: 1rem;
		box-shadow: 
			0 6px 25px rgb(26, 54, 93, 0.4),
			0 0 50px rgb(26, 54, 93, 0.3),
			0 0 80px rgb(26, 54, 93, 0.15);
	}

	.nomination-btn:hover {
		transform: translateY(-2px);
		box-shadow: 
			0 10px 35px rgb(0, 0, 0, 0.3),
			0 0 60px rgb(26, 54, 93, 0.6),
			0 0 100px rgb(26, 54, 93, 0.4),
			inset 0 0 30px rgb(26, 54, 93, 0.15);
	}

	@keyframes subtle-glow {
		from {
			box-shadow: 
				0 25px 60px rgb(0, 0, 0, 0.15),
				0 0 100px rgb(246, 173, 85, 0.25),
				0 0 150px rgb(26, 54, 93, 0.15),
				inset 0 0 50px rgb(246, 173, 85, 0.03);
			border-color: rgb(246, 173, 85, 0.2);
		}

		to {
			box-shadow: 
				0 25px 60px rgb(0, 0, 0, 0.15),
				0 0 120px rgb(246, 173, 85, 0.4),
				0 0 180px rgb(26, 54, 93, 0.25),
				inset 0 0 70px rgb(246, 173, 85, 0.08);
			border-color: rgb(246, 173, 85, 0.5);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.nomination-form {
			animation: none;
		}

		.form-group input,
		.form-group select,
		.form-group textarea,
		.nomination-btn {
			transition: none;
		}
	}

	@media (max-width: 768px) {
		h1.nomination-title {
			font-size: 2rem;
		}

		.form-row {
			grid-template-columns: 1fr;
		}

		.nomination-form {
			padding: 1.5rem;
		}

		.nomination-description h3 {
			font-size: 1.3rem;
		}
	}
</style>

<script>
	// Form handling with success message
	const nominationForm = document.getElementById('nominationForm') as HTMLFormElement;
	
	nominationForm?.addEventListener('submit', async function(e) {
		e.preventDefault();
		
		const submitButton = this.querySelector('button[type="submit"]') as HTMLButtonElement;
		const originalText = submitButton.textContent;
		
		// Show loading state
		submitButton.textContent = 'Wird gesendet...';
		submitButton.disabled = true;
		
		try {
			const formData = new FormData(this);
			const object = Object.fromEntries(formData);
			const json = JSON.stringify(object);
			
			// Send to Web3Forms
			const response = await fetch('https://api.web3forms.com/submit', {
				method: 'POST',
				headers: {
					'Content-Type': 'application/json',
					'Accept': 'application/json'
				},
				body: json
			});
			
			const result = await response.json();
			
			if (result.success) {
				// Track event before redirect
				if (typeof window.trackEvent === 'function') {
					window.trackEvent('form_submit', {
						event_category: 'engagement',
						event_label: 'nomination_form',
						form_type: 'nomination'
					});
				}
				
				// Redirect to thank you page
				window.location.href = '/danke-nominierung';
				
				// Reset form (in case redirect fails)
				this.reset();
			} else {
				throw new Error(result.message || 'Form submission failed');
			}
		} catch (error) {
			console.error('Form submission error:', error);
			
			// Show error message
			const errorMsg = document.getElementById('errorMessage') as HTMLElement;
			const errorText = document.getElementById('errorText') as HTMLElement;
			const successMsg = document.getElementById('successMessage') as HTMLElement;
			
			if (successMsg) successMsg.style.display = 'none';
			if (errorMsg && errorText) {
				errorText.textContent = 'Es gab einen Fehler beim Senden. Bitte versuchen Sie es erneut.';
				errorMsg.style.display = 'block';
				// Auto-hide after 7 seconds
				setTimeout(() => errorMsg.style.display = 'none', 7000);
			}
		} finally {
			// Reset button state
			submitButton.textContent = originalText;
			submitButton.disabled = false;
		}
	});
</script>