---
// Newsletter section component for Mobility Trailblazers
import SectionDivider from './SectionDivider.astro';
---

<section class="newsletter" id="newsletter">
	<SectionDivider type="curve" gradient="curveGradient" position="top" />
	<div class="container">
		<div class="newsletter-content">
			<h2 class="newsletter-title">Bleiben Sie informiert – mit unserem Mobility Trailblazers Newsletter</h2>
			
			<div class="newsletter-description">
				<p>Erfahren Sie zuerst, wer nominiert wurde, wie der Auswahlprozess läuft und was Jury und Medien über den Mobilitätswandel denken. Inklusive:</p>
				<ul>
					<li>Hintergrundgeschichten & Interviews</li>
					<li>News rund um das Event & das Netzwerk</li>
					<li>Das exklusive digitale Booklet mit allen Trailblazer-Porträts, Artikeln und Insights</li>
				</ul>
				<p><strong>Direkt nach dem Award erhalten Sie das komplette Booklet bequem per E-Mail.</strong></p>
			</div>
			
			<form class="newsletter-form" id="newsletterForm">
				<!-- Form status messages -->
				<div class="form-message success-message" id="successMessage" role="alert" aria-live="polite" style="display: none;">
					✓ Vielen Dank für Ihre Anmeldung zum Newsletter!
				</div>
				<div class="form-message error-message" id="errorMessage" role="alert" aria-live="assertive" style="display: none;">
					⚠ Es gab einen Fehler beim Senden. Bitte versuchen Sie es erneut.
				</div>
				
				<!-- Web3Forms Configuration -->
				<input type="hidden" name="access_key" value="3bae7bb8-eb67-4656-8ba3-900a0d95306a">
				<input type="hidden" name="subject" value="Neue Newsletter Anmeldung - Mobility Trailblazers">
				<input type="hidden" name="from_name" value="Mobility Trailblazers Website">
				
				<!-- Additional Web3Forms fields -->
				<!-- Redirect is commented out to use AJAX submission instead -->
				<!-- <input type="hidden" name="redirect" value="https://mobilitytrailblazers.de/#newsletter?success=true"> -->
				
				<!-- Email configuration for better deliverability -->
				<input type="hidden" name="replyto" value="email">
				
				<!-- Specify the email address where submissions should be sent -->
				<input type="hidden" name="email" value="kontakt@mobilitytrailblazers.de">
				
				<!-- Metadata for tracking -->
				<input type="hidden" name="_template" value="table">
				<input type="hidden" name="_captcha" value="false">
				
                                <div class="form-group">
                                        <label for="firstName">Vorname *</label>
                                        <input type="text" id="firstName" name="firstName" required>
                                        <p class="field-error" id="firstNameError"></p>
                                </div>

                                <div class="form-group">
                                        <label for="lastName">Nachname *</label>
                                        <input type="text" id="lastName" name="lastName" required>
                                        <p class="field-error" id="lastNameError"></p>
                                </div>

                                <div class="form-group">
                                        <label for="email">E-Mail-Adresse *</label>
                                        <input type="email" id="email" name="email" required>
                                        <p class="field-error" id="emailError"></p>
                                </div>
				
				<!-- Honeypot Spam Protection -->
				<input type="checkbox" name="botcheck" class="hidden" style="display: none;">
				
				<div class="form-group">
                                        <label for="salutation">Anrede *</label>
                                        <select id="salutation" name="salutation" required>
                                                <option value="">Bitte wählen</option>
                                                <option value="Frau">Frau</option>
                                                <option value="Herr">Herr</option>
                                                <option value="Keine Angabe">Keine Angabe</option>
                                        </select>
                                        <p class="field-error" id="salutationError"></p>
                                </div>
				
				<button type="submit" class="newsletter-btn">Newsletter abonnieren</button>
			</form>
		</div>
	</div>
</section>

<style>
	.newsletter {
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%);
		color: #fff;
		position: relative;
		padding: 3rem 0;
	}

	.newsletter::before {
		content: '';
		position: absolute;
		inset: 0;
		background: url('/images/background.webp') center center/cover;
		opacity: 0.1;
		z-index: 1;
	}

	.newsletter-content {
		max-width: 800px;
		margin: 0 auto;
		text-align: center;
		position: relative;
		z-index: 2;
	}

	.newsletter-title {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 2rem;
		background: linear-gradient(45deg, #ffffff, #f0f0f0);
		background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.newsletter-description {
		margin-bottom: 3rem;
		text-align: left;
	}

	.newsletter-description p {
		font-size: 1.1rem;
		line-height: 1.6;
		margin-bottom: 1rem;
	}

	.newsletter-description ul {
		list-style: none;
		padding: 0;
		margin: 1rem 0;
	}

	.newsletter-description li {
		padding: 0.5rem 0;
		font-size: 1rem;
		position: relative;
		padding-left: 1.5rem;
	}

	.newsletter-description li::before {
		content: '✓';
		position: absolute;
		left: 0;
		color: var(--color-accent);
		font-weight: bold;
	}

	.newsletter-form {
		background: rgb(255, 255, 255, 0.1);
		backdrop-filter: blur(10px);
		border-radius: 20px;
		padding: 2rem;
		border: 2px solid rgb(246, 173, 85, 0.3);
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.5rem;
		text-align: left;
		box-shadow: 
			0 20px 60px rgb(0, 0, 0, 0.2),
			0 0 80px rgb(246, 173, 85, 0.3),
			0 0 120px rgb(26, 54, 93, 0.2),
			inset 0 0 60px rgb(246, 173, 85, 0.05);
		animation: subtle-glow 3s ease-in-out infinite alternate;
	}

	.form-group {
		display: flex;
		flex-direction: column;
	}

	.form-group:nth-child(5),
	.form-group:nth-child(6) {
		grid-column: span 2;
	}

	.form-group:last-child {
		grid-column: span 2;
		text-align: center;
	}

	.form-group label {
		font-size: 0.9rem;
		font-weight: 600;
		margin-bottom: 0.5rem;
		color: rgb(255, 255, 255, 0.9);
	}

	.form-group input,
	.form-group select {
		padding: 0.8rem 1rem;
		border: 2px solid rgb(255, 255, 255, 0.3);
		border-radius: 8px;
		background: rgb(255, 255, 255, 0.1);
		color: #fff;
		font-size: 1rem;
		transition: all 0.3s ease;
		box-shadow: 
			0 0 10px rgb(26, 54, 93, 0.3),
			0 0 20px rgb(246, 173, 85, 0.2),
			0 0 30px rgb(26, 54, 93, 0.1);
	}

	.form-group input::placeholder {
		color: rgb(255, 255, 255, 0.6);
	}

	.form-group input:hover,
	.form-group select:hover {
		border-color: rgb(246, 173, 85, 0.8);
		box-shadow: 
			0 0 15px rgb(26, 54, 93, 0.5),
			0 0 30px rgb(246, 173, 85, 0.4),
			0 0 45px rgb(26, 54, 93, 0.3),
			0 0 60px rgb(246, 173, 85, 0.2);
	}

	.form-group input:focus,
	.form-group select:focus {
		outline: none;
		border-color: var(--color-accent);
		background: rgb(255, 255, 255, 0.3);
		box-shadow: 
			0 0 0 3px rgb(246, 173, 85, 0.6),
			0 0 20px rgb(26, 54, 93, 0.6),
			0 0 40px rgb(246, 173, 85, 0.5),
			0 0 60px rgb(26, 54, 93, 0.4),
			0 0 80px rgb(246, 173, 85, 0.3),
			inset 0 0 20px rgb(246, 173, 85, 0.1);
	}

        .form-group select option {
                background: var(--color-primary);
                color: #fff;
        }

        .field-error {
                color: #ffe0e0;
                font-size: 0.85rem;
                margin-top: 0.35rem;
                min-height: 1.2rem;
                display: none;
        }

	.newsletter-btn {
		background: var(--color-accent);
		color: #fff;
		border: none;
		padding: 1rem 2rem;
		border-radius: 50px;
		font-size: 1rem;
		font-weight: 600;
		cursor: pointer;
		transition: all 0.3s ease;
		margin-top: 1rem;
		box-shadow: 
			0 4px 20px rgb(26, 54, 93, 0.5),
			0 0 40px rgb(26, 54, 93, 0.3);
	}

	.form-message {
		grid-column: span 2;
		padding: 1rem;
		border-radius: 8px;
		margin-bottom: 1rem;
		font-size: 1rem;
		text-align: center;
	}

	.success-message {
		background: rgb(212, 237, 218, 0.9);
		border: 1px solid rgb(195, 230, 203, 0.9);
		color: #155724;
	}

	.error-message {
		background: rgb(248, 215, 218, 0.9);
		border: 1px solid rgb(245, 198, 203, 0.9);
		color: #721c24;
	}

	.newsletter-btn:hover {
		background: #fff;
		color: var(--color-accent);
		transform: translateY(-2px);
		box-shadow: 
			0 10px 30px rgb(0, 0, 0, 0.3),
			0 0 50px rgb(26, 54, 93, 0.7),
			0 0 100px rgb(26, 54, 93, 0.4),
			inset 0 0 30px rgb(26, 54, 93, 0.2);
	}

	@keyframes subtle-glow {
		from {
			box-shadow: 
				0 20px 60px rgb(0, 0, 0, 0.2),
				0 0 80px rgb(246, 173, 85, 0.3),
				0 0 120px rgb(26, 54, 93, 0.2),
				inset 0 0 60px rgb(246, 173, 85, 0.05);
			border-color: rgb(246, 173, 85, 0.3);
		}

		to {
			box-shadow: 
				0 20px 60px rgb(0, 0, 0, 0.2),
				0 0 100px rgb(246, 173, 85, 0.5),
				0 0 150px rgb(26, 54, 93, 0.3),
				inset 0 0 80px rgb(246, 173, 85, 0.1);
			border-color: rgb(246, 173, 85, 0.6);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.newsletter-form {
			animation: none;
		}

		.form-group input,
		.form-group select,
		.newsletter-btn {
			transition: none;
		}
	}

	@media (max-width: 768px) {
		.newsletter-title {
			font-size: 2rem;
		}

		.newsletter-form {
			grid-template-columns: 1fr;
			padding: 1.5rem;
		}

		.form-group:nth-child(5),
		.form-group:nth-child(6) {
			grid-column: span 1;
		}

		.form-group:last-child {
			grid-column: span 1;
		}
	}
</style>

<script>
	import { submitToWeb3Forms } from '../utils/formSubmission';

        // Form handling with validation and rate limiting
        const newsletterForm = document.getElementById('newsletterForm') as HTMLFormElement;

        const fieldConfig = {
                firstName: {
                        input: document.getElementById('firstName') as HTMLInputElement,
                        error: document.getElementById('firstNameError') as HTMLElement,
                        baseDescribedBy: (document.getElementById('firstName') as HTMLElement)?.getAttribute('aria-describedby')
                },
                lastName: {
                        input: document.getElementById('lastName') as HTMLInputElement,
                        error: document.getElementById('lastNameError') as HTMLElement,
                        baseDescribedBy: (document.getElementById('lastName') as HTMLElement)?.getAttribute('aria-describedby')
                },
                email: {
                        input: document.getElementById('email') as HTMLInputElement,
                        error: document.getElementById('emailError') as HTMLElement,
                        baseDescribedBy: (document.getElementById('email') as HTMLElement)?.getAttribute('aria-describedby')
                },
                salutation: {
                        input: document.getElementById('salutation') as HTMLSelectElement,
                        error: document.getElementById('salutationError') as HTMLElement,
                        baseDescribedBy: (document.getElementById('salutation') as HTMLElement)?.getAttribute('aria-describedby')
                }
        };

        const updateDescribedBy = (
                element: HTMLElement,
                errorId: string,
                isActive: boolean,
                baseDescribedBy: string | null | undefined
        ) => {
                const baseIds = baseDescribedBy ? baseDescribedBy.split(' ').filter(Boolean) : [];
                const ids = new Set(baseIds);

                if (isActive) {
                        ids.add(errorId);
                } else {
                        ids.delete(errorId);
                }

                if (ids.size > 0) {
                        element.setAttribute('aria-describedby', Array.from(ids).join(' '));
                } else {
                        element.removeAttribute('aria-describedby');
                }
        };

        const clearFieldErrors = () => {
                Object.values(fieldConfig).forEach(({ input, error, baseDescribedBy }) => {
                        if (error) {
                                error.textContent = '';
                                error.style.display = 'none';
                        }
                        if (input) {
                                input.removeAttribute('aria-invalid');
                                updateDescribedBy(input, error.id, false, baseDescribedBy);
                        }
                });
        };

        const showFieldErrors = (errors: Map<string, string>) => {
                errors.forEach((message, field) => {
                        const config = fieldConfig[field as keyof typeof fieldConfig];
                        if (!config) return;

                        const { input, error, baseDescribedBy } = config;
                        if (error) {
                                error.textContent = message;
                                error.style.display = 'block';
                        }

                        if (input) {
                                input.setAttribute('aria-invalid', 'true');
                                updateDescribedBy(input, error.id, true, baseDescribedBy);
                        }
                });
        };

        newsletterForm?.addEventListener('submit', async function(e) {
                e.preventDefault();

                const successMsg = document.getElementById('successMessage') as HTMLElement;
                const errorMsg = document.getElementById('errorMessage') as HTMLElement;

                clearFieldErrors();

                try {
                        await submitToWeb3Forms({
                                form: this,
                                successRedirect: '/danke-newsletter',
                                eventLabel: 'newsletter_form',
                                onValidationError: ({ errors }) => {
                                        if (successMsg) successMsg.style.display = 'none';
                                        if (errorMsg) errorMsg.style.display = 'none';
                                        showFieldErrors(errors);
                                },
                                onError: (errorMessage) => {
                                        // Show error message
                                        if (successMsg) successMsg.style.display = 'none';
                                        if (errorMsg) {
                                                errorMsg.textContent = `⚠ ${errorMessage}`;
                                                errorMsg.style.display = 'block';
                                                // Auto-hide after 7 seconds
                                                setTimeout(() => errorMsg.style.display = 'none', 7000);
                                        }
                                }
                        });
                } catch (error) {
                        // Error already handled by utility
                }
        });
</script>
