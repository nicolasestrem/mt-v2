---
// Newsletter section component for Mobility Trailblazers
import { getContent } from '../lib/tina';

interface NewsletterContent {
  title: string;
  subtitle: string;
  description: string;
  buttonText: string;
  placeholderText: string;
}

// Load newsletter content from static JSON files with fallback defaults
const content = getContent<NewsletterContent>('newsletter', 'newsletter') || {
  title: "Bleiben Sie informiert – mit unserem Mobility Trailblazers Newsletter",
  subtitle: "Erhalten Sie Updates zum Award und zur Mobilitätswende",
  description: "Melden Sie sich für unseren Newsletter an und verpassen Sie keine wichtigen Updates zu den 25 Mobility Trailblazers in 25 und aktuellen Entwicklungen in der Mobilität.",
  buttonText: "Newsletter abonnieren",
  placeholderText: "Ihre E-Mail-Adresse"
};
---

<section class="newsletter" id="newsletter">
	<!-- Subtle curve divider at top -->
	<div class="section-divider-top">
		<svg viewBox="0 0 1200 80" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
			<defs>
				<linearGradient id="curveGradient" x1="0%" y1="0%" x2="100%" y2="0%">
					<stop offset="0%" style="stop-color:#1a365d;stop-opacity:1" />
					<stop offset="50%" style="stop-color:#253c52;stop-opacity:1" />
					<stop offset="100%" style="stop-color:#2d3748;stop-opacity:1" />
				</linearGradient>
			</defs>
			<path d="M0,80 Q600,0 1200,80 L1200,0 L0,0 Z" fill="url(#curveGradient)"></path>
		</svg>
	</div>
	<div class="container">
		<div class="newsletter-content">
			<h2 class="newsletter-title" style="color: #C1693C;">{content.title}</h2>
			
			<div class="newsletter-description">
				<p>Erfahren Sie zuerst, wer nominiert wurde, wie der Auswahlprozess läuft und was Jury und Medien über den Mobilitätswandel denken. Inklusive:</p>
				<ul>
					<li>Hintergrundgeschichten & Interviews</li>
					<li>News rund um das Event & das Netzwerk</li>
					<li>Das exklusive digitale Booklet mit allen Trailblazer-Porträts, Artikeln und Insights</li>
				</ul>
				<p><strong>Direkt nach dem Award erhalten Sie das komplette Booklet bequem per E-Mail.</strong></p>
			</div>
			
			<form class="newsletter-form" id="newsletterForm">
				<div class="form-group">
					<label for="firstName">Vorname *</label>
					<input type="text" id="firstName" name="firstName" required>
				</div>
				
				<div class="form-group">
					<label for="lastName">Nachname *</label>
					<input type="text" id="lastName" name="lastName" required>
				</div>
				
				<div class="form-group">
					<label for="email">E-Mail-Adresse *</label>
					<input type="email" id="email" name="email" required>
				</div>
				
				<div class="form-group">
					<label for="salutation">Anrede *</label>
					<select id="salutation" name="salutation" required>
						<option value="">Bitte wählen</option>
						<option value="Frau">Frau</option>
						<option value="Herr">Herr</option>
						<option value="Keine Angabe">Keine Angabe</option>
					</select>
				</div>
				
				<button type="submit" class="newsletter-btn">Newsletter abonnieren</button>
			</form>
		</div>
	</div>
</section>

<style>
	.newsletter {
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%);
		color: white;
		position: relative;
		padding: 3rem 0;
	}

	.section-divider-top {
		position: absolute;
		top: -1px;
		left: 50%;
		transform: translateX(-50%);
		width: 100%;
		max-width: none;
		overflow: hidden;
		line-height: 0;
		z-index: 2;
	}

	.section-divider-top svg {
		position: relative;
		display: block;
		width: 100%;
		height: 40px;
	}

	@media (min-width: 768px) {
		.section-divider-top svg {
			height: 60px;
		}
	}

	.newsletter::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: url('/images/background.webp') center center/cover;
		opacity: 0.1;
		z-index: 1;
	}

	.newsletter-content {
		max-width: 800px;
		margin: 0 auto;
		text-align: center;
		position: relative;
		z-index: 2;
	}

	.newsletter-title {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 2rem;
		background: linear-gradient(45deg, #ffffff, #f0f0f0);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.newsletter-description {
		margin-bottom: 3rem;
		text-align: left;
	}

	.newsletter-description p {
		font-size: 1.1rem;
		line-height: 1.6;
		margin-bottom: 1rem;
	}

	.newsletter-description ul {
		list-style: none;
		padding: 0;
		margin: 1rem 0;
	}

	.newsletter-description li {
		padding: 0.5rem 0;
		font-size: 1rem;
		position: relative;
		padding-left: 1.5rem;
	}

	.newsletter-description li::before {
		content: '✓';
		position: absolute;
		left: 0;
		color: var(--color-accent);
		font-weight: bold;
	}

	.newsletter-form {
		background: rgba(255, 255, 255, 0.1);
		backdrop-filter: blur(10px);
		border-radius: 20px;
		padding: 2rem;
		border: 2px solid rgba(246, 173, 85, 0.3);
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.5rem;
		text-align: left;
		box-shadow: 
			0 20px 60px rgba(0, 0, 0, 0.2),
			0 0 80px rgba(246, 173, 85, 0.3),
			0 0 120px rgba(26, 54, 93, 0.2),
			inset 0 0 60px rgba(246, 173, 85, 0.05);
		animation: subtle-glow 3s ease-in-out infinite alternate;
	}

	.form-group {
		display: flex;
		flex-direction: column;
	}

	.form-group:nth-child(3),
	.form-group:nth-child(4) {
		grid-column: span 2;
	}

	.form-group:last-child {
		grid-column: span 2;
		text-align: center;
	}

	.form-group label {
		font-size: 0.9rem;
		font-weight: 600;
		margin-bottom: 0.5rem;
		color: rgba(255, 255, 255, 0.9);
	}

	.form-group input,
	.form-group select {
		padding: 0.8rem 1rem;
		border: 2px solid rgba(255, 255, 255, 0.3);
		border-radius: 8px;
		background: rgba(255, 255, 255, 0.1);
		color: white;
		font-size: 1rem;
		transition: all 0.3s ease;
		box-shadow: 
			0 0 10px rgba(26, 54, 93, 0.3),
			0 0 20px rgba(246, 173, 85, 0.2),
			0 0 30px rgba(26, 54, 93, 0.1);
	}

	.form-group input::placeholder {
		color: rgba(255, 255, 255, 0.6);
	}

	.form-group input:hover,
	.form-group select:hover {
		border-color: rgba(246, 173, 85, 0.8);
		box-shadow: 
			0 0 15px rgba(26, 54, 93, 0.5),
			0 0 30px rgba(246, 173, 85, 0.4),
			0 0 45px rgba(26, 54, 93, 0.3),
			0 0 60px rgba(246, 173, 85, 0.2);
	}

	.form-group input:focus,
	.form-group select:focus {
		outline: none;
		border-color: var(--color-accent);
		background: rgba(255, 255, 255, 0.3);
		box-shadow: 
			0 0 0 3px rgba(246, 173, 85, 0.6),
			0 0 20px rgba(26, 54, 93, 0.6),
			0 0 40px rgba(246, 173, 85, 0.5),
			0 0 60px rgba(26, 54, 93, 0.4),
			0 0 80px rgba(246, 173, 85, 0.3),
			inset 0 0 20px rgba(246, 173, 85, 0.1);
	}

	.form-group select option {
		background: var(--color-primary);
		color: white;
	}

	.newsletter-btn {
		background: var(--color-accent);
		color: white;
		border: none;
		padding: 1rem 2rem;
		border-radius: 50px;
		font-size: 1rem;
		font-weight: 600;
		cursor: pointer;
		transition: all 0.3s ease;
		margin-top: 1rem;
		box-shadow: 
			0 4px 20px rgba(26, 54, 93, 0.5),
			0 0 40px rgba(26, 54, 93, 0.3);
	}

	.newsletter-btn:hover {
		background: white;
		color: var(--color-accent);
		transform: translateY(-2px);
		box-shadow: 
			0 10px 30px rgba(0, 0, 0, 0.3),
			0 0 50px rgba(26, 54, 93, 0.7),
			0 0 100px rgba(26, 54, 93, 0.4),
			inset 0 0 30px rgba(26, 54, 93, 0.2);
	}

	@keyframes subtle-glow {
		from {
			box-shadow: 
				0 20px 60px rgba(0, 0, 0, 0.2),
				0 0 80px rgba(246, 173, 85, 0.3),
				0 0 120px rgba(26, 54, 93, 0.2),
				inset 0 0 60px rgba(246, 173, 85, 0.05);
			border-color: rgba(246, 173, 85, 0.3);
		}
		to {
			box-shadow: 
				0 20px 60px rgba(0, 0, 0, 0.2),
				0 0 100px rgba(246, 173, 85, 0.5),
				0 0 150px rgba(26, 54, 93, 0.3),
				inset 0 0 80px rgba(246, 173, 85, 0.1);
			border-color: rgba(246, 173, 85, 0.6);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.newsletter-form {
			animation: none;
		}
		.form-group input,
		.form-group select,
		.newsletter-btn {
			transition: none;
		}
	}

	@media (max-width: 768px) {
		.newsletter-title {
			font-size: 2rem;
		}

		.newsletter-form {
			grid-template-columns: 1fr;
			padding: 1.5rem;
		}

		.form-group:nth-child(3),
		.form-group:nth-child(4) {
			grid-column: span 1;
		}

		.form-group:last-child {
			grid-column: span 1;
		}
	}
</style>

<script>
	// Newsletter form handling
	const newsletterForm = document.getElementById('newsletterForm') as HTMLFormElement;
	
	newsletterForm?.addEventListener('submit', function(e) {
		e.preventDefault();
		
		const formData = new FormData(this);
		const data = Object.fromEntries(formData);
		
		// Here you would typically send the data to your backend
		// TODO: Integrate with newsletter service API
		
		// Show success message
		alert('Vielen Dank für Ihre Anmeldung zum Newsletter!');
		
		// Reset form
		this.reset();
	});
</script>
